name: brew_restaurant_menu
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  carousel_slider: ^4.2.1
  fast_cached_network_image: ^1.2.0
  flutter:
    sdk: flutter
  google_fonts: ^5.1.0
  http: ^1.1.0
  image_network: ^2.5.4+1
  provider: ^6.0.5
  url_launcher: ^6.1.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/